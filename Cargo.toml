[package]
name = "botkit"
version = "0.1.0"
edition.workspace = true
license.workspace = true
readme.workspace = true
repository.workspace = true
description = "Unified Rust bot framework with optional Discord, Telegram, and Matrix adapters"
autoexamples = false
keywords = ["bot", "discord", "telegram", "matrix"]
categories = ["asynchronous", "api-bindings"]

[workspace]
resolver = "2"
members = ["core", "discord", "telegram", "matrix", "examples"]

[workspace.package]
edition = "2024"
license = "MIT OR Apache-2.0"
readme = "README.md"
repository = "https://github.com/lexoliu/botkit"

[workspace.dependencies]
# Internal crates
botkit-core = { version = "0.1.0", path = "core" }
botkit-discord = { version = "0.1.0", path = "discord" }
botkit-telegram = { version = "0.1.0", path = "telegram" }
botkit-matrix = { version = "0.1.0", path = "matrix" }

# HTTP foundation
http-kit = "0.4.2"
zenwave = "0.3.0"
# Pinned: skyzen 0.1.1 pulls in skyzen-services -> sqlx -> libsqlite3-sys 0.28,
# which cannot coexist with matrix-sdk's rusqlite (libsqlite3-sys 0.35) in one
# dependency graph. skyzen-core is pinned alongside it because 0.1.1 added a
# field to `ExtractorSchema` that skyzen 0.1.0 does not set, and skyzen-macros
# 0.1.1 expects a `skyzen::import_config` that 0.1.0 does not export, so all
# three only compile as a matched set.
skyzen = "=0.1.0"
skyzen-core = "=0.1.0"
skyzen-macros = "=0.1.0"

# Serialization
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.148"

# Error handling
thiserror = "2.0.17"

# Async runtime
futures-lite = "2"
executor-core = "0.7"

[features]
default = []
discord = ["dep:botkit-discord"]
telegram = ["dep:botkit-telegram"]
matrix = ["dep:botkit-matrix"]
full = ["discord", "telegram", "matrix"]

[dependencies]
botkit-core.workspace = true
botkit-discord = { workspace = true, optional = true }
botkit-telegram = { workspace = true, optional = true }
botkit-matrix = { workspace = true, optional = true }
