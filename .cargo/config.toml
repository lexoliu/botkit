# getrandom has no default entropy source on wasm32-unknown-unknown: it needs
# both the `wasm_js` feature and this backend cfg, or the build fails with
# "the wasm32-unknown-unknown targets are not supported by default".
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
